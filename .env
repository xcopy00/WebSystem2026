# Binance AI Trade System - Environment Configuration
# IMPORTANT: Copy this to .env and fill in your MySQL credentials!

# Database Configuration (MySQL)
DB_HOST=localhost
DB_NAME=ai_trading_bot
DB_USER=root
DB_PASS=

# Application Configuration
APP_URL=http://localhost

# SECURITY: Generate a strong random secret for JWT
# Run: php -r "echo bin2hex(random_bytes(32));"
JWT_SECRET=change-this-to-a-secure-random-string
